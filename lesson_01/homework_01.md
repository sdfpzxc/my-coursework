Домашнее задание 1
Имя: Геращенко Ксения
Группа: РПО2/3
Дата:09.09.2026

1. Разбор сервиса Додо Пицца

Итак, конструктор пиццы в первую очередь я думаю

На сервер уходит: recipe_id (это нужно писать? я помню вы просили без кода, и это вроде не считается кодом, поэтому я на всякий напишу в тако формате тоже, могу ошибаться, но надеюсь я все правильно запомнила с пары так что да ..), список начинок, отметки о том что закончились ингредиенты, идентификатор сессий
Сервер проверяет: авторизацию, наличие начинок, лимит (например до 7 или 5 это неважно), наличие ингредиентов на кухне
Сохранение: корзина в redis (кеш), справочник пицц
Ответ: итоговая цена, вес, может калорийность, обновленное изображение пиццы (если замена пиццы например), новая сумма корзины

2.Оплата бонусами

На сервер уходит: cart_id, способ оплаты, количество бонусов для списания
Сервер проверяет: достаточно ли бонусов, не превышает ли списание сумму заказа, минимальный порог оплаты, актуальность корзины
Сохранение: заказ в orders (статус "получен"), списание бонусов в bonus_transactions(как то так это записать??), отправка в очередь на кухню
Ответ: номер заказа, время приготовления, остаток бонусов, ссылка на трекер курьера

3. История заказов

На сервер уходит: user_id (из токена), page и limit для пагинации(это разделение большого объема контента на отдельные страницы с возможностью перехода между ними )
Сервер проверяет: валидность сессии (чужая история недоступна)
Сохранение: чтение из PostgreSQL(это сис управления базами данных  с открытым кодом) (orders_history), кеширование в redis для частых запросов.
Ответ: список заказов (дата, сумма, статус) и ри нажатии повторить - полный состав корзины для быстрого повторного заказа

4. Место для отказа (оплата с бонусами)

Вот тестик сценария оплаты в приложении Додо пицца( у меня бзв 997 бонусов там ):

Пропажа сети: при отключении ви-фи в момент оплаты приложение показывает спиннер 15 секунд, затем ошибку не удалось обработать заказ, по итогу заказ не создается, бонусы не списываются и транзакция откатилась по таймауту
Двойное нажатие: кнопка блокируется после первого клика, на сервер уходит только один запрос, защита: фронтенд блокировка + idempotency_key( это уникальный идентификатор, который клиент генерирует для конкретной логической операции и передает серверу вместе с запросом ) на сервере
Пустой коммент: поле опционально, заказ проходит успешно и если ввести только пробелы то сервер возвращает ошибку что комментарий не может состоять из пробелов вот
Списание бонусов больше, чем есть: тогда фронтенд ограничивает ввод, при подмене запроса сервер возвращает ошибку 400 типо недостаточно бонусов, двойная проверка
Вообщем приложение надежно защищено от сбоев и ошибок пользователя вот так вот




